#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - Entry point.
* Return: 0 (success).
*/

int main(void)
{
for (char n = a; a <= z, n++)
{
putchar(n);
}
for (char m = A; A <= Z, m++)
{
putchar(m);
}
putchar('\n');
return (0);
}
