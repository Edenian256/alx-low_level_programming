#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point.
 * Return: 0 (success).
 */
int main(void)
{
int n = 28;
while (n < 38)
{
putchar(n);
n++;
}
putchar('\n');
return (0);
}
